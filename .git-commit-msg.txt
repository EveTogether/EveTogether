- fixed: ET-285 round 3 the activity detail's content collapsed to 470-550px (both activities) instead of filling up to the 1040px cap, once round 2 shortened the absent-sections footnote enough that nothing was left wide enough to reach 1040px on its own — root cause was round 1's HorizontalAlignment="Left" on the content DockPanel: a non-Stretch alignment reports back only the width its content actually needs rather than filling the space it is measured with, so the DockPanel had been shrinking to content all along and round 1's renders only looked like they filled the cap because the still-long two-line footnote coincidentally needed close to 1040px on its own; dropped Left, plain MaxWidth="1040" with the default Stretch is the actual "min(available, 1040), filling up to it" rule — verified this time by reading DockPanel.Bounds.Width directly (1040 for both activities) before saving either render, not just by eye; content now centers within a wider host once capped instead of sitting flush left, one of the two placements the ticket named as fine ("gecentreerd of links"); confirmed the run window's own copy of ActivityLootView (600px, unaffected by anything in this fix) still renders its LOOT table and the round-2 GROUP TOTAL style correctly
