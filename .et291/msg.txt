- added: ET-291 the selected run reads in a pane of its own — beside the list from 1200 px up (RunsLayout.WideFrom, taken from the band RO-3 and RO-4 put above the list: a 400 px strip plus two filter blocks is about 1204), and in a 430 px drawer over the dimmed list below it. One RunsActivityPaneViewModel and one RunsActivityPane view in two hosts, of which exactly one is ever shown, so widening is not a second design
- added: ET-291 the runs content root is a Grid of three layers — the screen, the scrim, the drawer. Borders in that Grid rather than a Popup or the OverlayLayer, which hang off a TopLevel that ModuleHostService re-parents this content out from under; and siblings of the DockPanel rather than a second undocked child of it (ET-285). The pane's column is explicit and never Auto, set from the content root's own bounds (the FleetsWindow observer pattern), so a docked tab and a floating window each answer for the width they were actually handed
- added: ET-291 the selection hangs on the view model, not on ListBox.SelectedItem: a folded day (ET-290) takes its rows out of the tab's flat Items and the list drops its selection, and that null is never read as "nothing is selected". ListSelection is what the list binds to and is the row only while its day is unfolded; _SettleSelection re-finds the activity by ActivitySummaryId after a refresh replaces the row instance, and drops it only where the activity has left the tab altogether
- added: ET-291 the arrow keys step over the activity rows of unfolded days, past pilots' runs and day headers, and over a folded day without unfolding it (besluit Jithran, 15 sep) — walked over the view model's own days rather than the realised containers, so a row outside the viewport is reachable too
- added: ET-291 DrawerSurfaceBrush #FF0C100E and ScrimBrush #AD02050A, neutral in all four factions like the ISK source inks: BgPanelBrush is 94 % and let the list show through the drawer, which took its dim labels under 4.5:1. The drawer's own edge is AccentBrightBrush, since border-s measured 2.3:1 against the scrim on Minmatar. StickyDay's hard-coded #FF0C100E is now that brush
- changed: ET-291 a click on a row selects it (SelectCommand) instead of opening the ET-162 detail screen, which is now a double-click, Enter or the pane's OPEN DETAIL. PUBLISH and RETRY move out of the row's 24 px sync cell — RO-1's hover-revealed stopgap — into the pane's sticky action bar
