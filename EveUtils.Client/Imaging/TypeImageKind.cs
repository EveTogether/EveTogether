namespace EveUtils.Client.Imaging;

/// <summary>Which CCP image-server asset to fetch for a type: a flat <c>icon</c> (modules, charges, drones) or the 3D
/// <c>render</c> (ships, structures).</summary>
public enum TypeImageKind
{
    Icon,
    Render
}
