namespace EveUtils.Client.ViewModels.Skills.Plans;

/// <summary>The PLANS tab's ORDER choice (ET-355) — a display setting, not stored on the plan itself.</summary>
public enum SkillPlanOrderMode
{
    FlyFirst,
    ShortestFirst,
    ByAttribute
}
