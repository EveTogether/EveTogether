namespace EveUtils.Client.ViewModels.Runs;

/// <summary>What the activity strip's cells are shaded by (ET-292). ISK is the default: the question the strip answers
/// first is which evenings paid.</summary>
public enum RunsStripShade
{
    Isk,
    Runs
}
