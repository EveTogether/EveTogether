<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:app="using:EveUtils.Client"
        xmlns:vm="using:EveUtils.Client.ViewModels"
        xmlns:dlg="using:EveUtils.Client.Dialogs"
        xmlns:ctl="using:EveUtils.Client.Controls"
        xmlns:home="using:EveUtils.Client.Views.Home"
        xmlns:chrome="using:Avalonia.Controls.Chrome"
        xmlns:mi="clr-namespace:Material.Icons.Avalonia;assembly=Material.Icons.Avalonia"
        xmlns:ships="using:EveUtils.Shared.Modules.Ships.Dtos"
        xmlns:settings="using:EveUtils.Shared.Modules.Settings.Dtos"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="1100" d:DesignHeight="720"
        x:Class="EveUtils.Client.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        x:Name="RootWindow"
        Icon="/Assets/eve-together.ico"
        Width="1100" Height="720" MinHeight="400"
        WindowStartupLocation="CenterScreen"
        Title="EVE Together — Client">

    <Design.DataContext>
        <vm:MainWindowViewModel/>
    </Design.DataContext>

    <Window.Styles>
        <!-- The rail's ten launcher icons were StreamGeometry in this window's resources (D-182); they come from the
             icon set now (ET-74). One size here rather than on each button, for the same reason it was one grid
             before: ten shapes that size themselves are ten weights. They take their colour by inheritance, so the
             faction/active/hover tint reaches them through the button's ContentPresenter.

             20 rather than the 22 the drawn Paths used: Material's outlines carry more ink than a 1.7 stroke, so at
             the old size the rail sat visibly heavier than the rest of the window. -->
        <!-- A portrait on the docked character rail (ET-324): no fill of its own, the hex ringed in the bright accent
             on hover and keyboard focus, the rail's "+" lit the same way. -->
        <Style Selector="Button.railportrait">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="MinHeight" Value="0"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{DynamicResource TextAccentBrush}"/>
        </Style>
        <Style Selector="Button.railportrait:pointerover /template/ ContentPresenter, Button.railportrait:pressed /template/ ContentPresenter">
            <Setter Property="Background" Value="Transparent"/>
        </Style>
        <Style Selector="Button.railportrait:pointerover ctl|HexPortrait, Button.railportrait:focus-visible ctl|HexPortrait">
            <Setter Property="Ring" Value="{DynamicResource AccentBrightBrush}"/>
        </Style>
        <Style Selector="Button.railportrait:pointerover Border, Button.railportrait:focus-visible Border">
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrightBrush}"/>
        </Style>

        <Style Selector="mi|MaterialIcon.rail">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
        </Style>
    </Window.Styles>

    <DockPanel>

        <!-- ── Title bar (borderless chrome, D-143): badge brand + responsive window buttons; whole bar drags.
             The TitleBar element role makes the drag a NATIVE caption drag on Windows (extend-client-area mode),
             which is what makes Aero Snap work; the pointer handlers stay for Linux/macOS. ── -->
        <Border DockPanel.Dock="Top" Height="{Binding TitleBarHeight}" Classes="titlebar" x:Name="TitleBar"
                chrome:WindowDecorationProperties.ElementRole="TitleBar"
                PointerPressed="OnTitleBarPressed" DoubleTapped="OnTitleBarDoubleTapped">
            <Grid ColumnDefinitions="*,Auto">
                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="10" Margin="14,0"
                            VerticalAlignment="Center" HorizontalAlignment="{Binding BrandAlignment}">
                    <Image Height="{Binding BrandLogoHeight}" Source="avares://EveUtils.Client/Assets/eve-together-badge.png"
                           RenderOptions.BitmapInterpolationMode="HighQuality" VerticalAlignment="Center"/>
                    <TextBlock Text="EVE TOGETHER" FontWeight="Bold" FontSize="13" LetterSpacing="2.2"
                               IsVisible="{Binding ShowBrandText}"
                               Foreground="{StaticResource TextBrightBrush}" VerticalAlignment="Center"/>
                    <TextBlock Text="{x:Static app:AppBranding.ReleaseStage}" FontSize="9" LetterSpacing="2.5" Opacity="0.8"
                               IsVisible="{Binding ShowBrandText}"
                               Foreground="{DynamicResource TextAccentBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Stretch"
                            IsVisible="{Binding ShowHeaderWindowButtons}">
                    <!-- User role = clickable on top of the caption area; the maximize button gets the native
                         MaximizeButton role so Windows 11 shows Snap Layouts when hovering it. -->
                    <Button Classes="winbtn" Click="OnMinimize" ToolTip.Tip="Minimize"
                            chrome:WindowDecorationProperties.ElementRole="User">
                        <mi:MaterialIcon Kind="WindowMinimize"/>
                    </Button>
                    <Button Classes="winbtn" Click="OnMaximize" ToolTip.Tip="Maximize / restore"
                            IsVisible="{Binding ShowMaximizeButton}"
                            chrome:WindowDecorationProperties.ElementRole="MaximizeButton">
                        <mi:MaterialIcon Kind="WindowMaximize"/>
                    </Button>
                    <Button Classes="winbtn winclose" Click="OnCloseWindow" ToolTip.Tip="Close"
                            chrome:WindowDecorationProperties.ElementRole="User">
                        <mi:MaterialIcon Kind="Close"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ── Downtime banner (D-157) ── -->
        <Border DockPanel.Dock="Top" IsVisible="{Binding IsEsiAlert}" Background="#33CC4444"
                BorderBrush="{StaticResource RedBrush}" BorderThickness="0,0,0,1" Padding="14,8">
            <!-- Grid, not a horizontal StackPanel: a StackPanel measures its children with unbounded width, so the
                 TextWrapping below never fired and a long banner simply ran off the right edge (ET-121). -->
            <Grid ColumnDefinitions="Auto,*">
                <mi:MaterialIcon Grid.Column="0" Classes="icon-lg" Kind="AlertOutline" Margin="0,0,10,0"
                                 VerticalAlignment="Center" Foreground="{StaticResource RedBrush}"/>
                <TextBlock Grid.Column="1" Text="{Binding EsiAlertMessage}" Foreground="{StaticResource TextBrightBrush}"
                           FontSize="12" VerticalAlignment="Center" TextWrapping="Wrap"/>
            </Grid>
        </Border>

        <!-- ── Lapsed server pairing (ET-77) ── Stays put until the pairing is valid again, like the update banner
             and unlike a toast: a list read against a server that refuses the session comes back EMPTY rather than
             failing, so the app would otherwise go on showing "there is nothing here" indefinitely. ── -->
        <Border DockPanel.Dock="Top" IsVisible="{Binding IsServerPairingAlert}" Background="#33CC4444"
                BorderBrush="{StaticResource RedBrush}" BorderThickness="0,0,0,1" Padding="14,8">
            <Grid ColumnDefinitions="Auto,*">
                <mi:MaterialIcon Grid.Column="0" Classes="icon-lg" Kind="CloudOffOutline" Margin="0,0,10,0"
                                 VerticalAlignment="Center" Foreground="{StaticResource RedBrush}"/>
                <TextBlock Grid.Column="1" Text="{Binding ServerPairingAlertMessage}" Foreground="{StaticResource TextBrightBrush}"
                           FontSize="12" VerticalAlignment="Center" TextWrapping="Wrap"/>
            </Grid>
        </Border>

        <!-- ── Refused server certificate (ET-95) ── The reconnect loop stops on this instead of retrying, so without
             the banner the link would simply go quiet. It carries the two fingerprints because deciding whether the
             certificate legitimately changed is the user's call and nothing else can make it for them. ── -->
        <Border DockPanel.Dock="Top" IsVisible="{Binding IsServerCertificateAlert}" Background="#33CC4444"
                BorderBrush="{StaticResource RedBrush}" BorderThickness="0,0,0,1" Padding="14,8">
            <Grid ColumnDefinitions="Auto,*">
                <mi:MaterialIcon Grid.Column="0" Classes="icon-lg" Kind="ShieldAlertOutline" Margin="0,0,10,0"
                                 VerticalAlignment="Center" Foreground="{StaticResource RedBrush}"/>
                <SelectableTextBlock Grid.Column="1" Text="{Binding ServerCertificateAlertMessage}" Foreground="{StaticResource TextBrightBrush}"
                                     FontSize="12" VerticalAlignment="Center" TextWrapping="Wrap"/>
            </Grid>
        </Border>

        <!-- ── Update-ready banner (ET-32) ── A downloaded package that is never applied leaves the app running the
             old build forever, so this stays put until the user restarts or dismisses it — unlike the offer toast,
             which only has to carry news. ── -->
        <Border DockPanel.Dock="Top" IsVisible="{Binding IsUpdateReady}" Background="{DynamicResource AccentSoftBrush}"
                BorderBrush="{DynamicResource AccentBrightBrush}" BorderThickness="0,0,0,1" Padding="14,8">
            <Grid ColumnDefinitions="Auto,*,Auto">
                <mi:MaterialIcon Grid.Column="0" Classes="icon-lg" Kind="Check"
                                 Foreground="{DynamicResource AccentBrightBrush}"/>
                <TextBlock Grid.Column="1" Text="{Binding UpdateReadyMessage}" Margin="10,0,0,0"
                           Foreground="{StaticResource TextBrightBrush}" FontSize="12" VerticalAlignment="Center"
                           TextWrapping="Wrap"/>
                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                    <Button Content="Restart now" Classes="accent" Command="{Binding RestartForUpdateCommand}"/>
                    <Button Content="Later" Command="{Binding DismissUpdateReadyCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ── Status bar: transient activity (left) + live Tranquility status (right). Hidden in floating (too narrow);
             a compact status dot in the rail replaces it there. ── -->
        <Border DockPanel.Dock="Bottom" x:Name="StatusBar" BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0" Padding="12,7" IsVisible="{Binding !IsFloating}">
            <Grid ColumnDefinitions="*,Auto,Auto">
                <TextBlock Grid.Column="0" Text="{Binding ActivityStatus}" VerticalAlignment="Center"
                           Foreground="{DynamicResource TextAccentBrush}" FontSize="11"/>
                <!-- Clipboard watch state. It sits in the always-visible strip rather than in settings: a feature
                     that can see everything you copy has to be readable as off without going looking for it. -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center"
                            Margin="0,0,14,0" ToolTip.Tip="{Binding ClipboardStatusTooltip}"
                            Background="Transparent" Cursor="Hand" Tapped="OnClipboardStatusTapped">
                    <Panel Width="9" Height="9" VerticalAlignment="Center">
                        <Ellipse Width="9" Height="9" Fill="{DynamicResource AccentBrightBrush}"
                                 IsVisible="{Binding IsClipboardWatching}"/>
                        <Ellipse Width="9" Height="9" Fill="#6B7280" IsVisible="{Binding !IsClipboardWatching}"/>
                    </Panel>
                    <TextBlock Text="{Binding ClipboardStatus}" VerticalAlignment="Center" FontSize="11"
                               LetterSpacing="1" Opacity="0.85"/>
                </StackPanel>
                <TextBlock Grid.Column="2" Text="{Binding TranquilityStatus}" VerticalAlignment="Center"
                           Foreground="{Binding TranquilityBrush}" FontSize="11" FontWeight="SemiBold" LetterSpacing="1"
                           ToolTip.Tip="EVE Tranquility server status (ESI /status/, refreshed every 30s)."/>
            </Grid>
        </Border>

        <!-- ── Module shell (D-182): rail (launcher) · character column · module host ── -->
        <Grid x:Name="BodyGrid" ColumnDefinitions="Auto,250,*">

            <!-- ── Rail: monochrome icon launcher + collapse toggle + rail-only window controls. The fixed width is
                 lifted in rail-only mode (ApplyShellState): Windows enforces a minimum window width on resizable
                 frames, so the rail stretches to fill it instead of leaving a dead strip beside itself. ── -->
            <Border Grid.Column="0" Classes="rail" Width="92" x:Name="Rail">
                <DockPanel>
                    <!-- Bottom: dock/float + chars toggles (always reachable) + (rail-only) window controls -->
                    <StackPanel x:Name="RailBottomBlock" DockPanel.Dock="Bottom" Spacing="0">
                        <Border Classes="rail-divider"/>
                        <Button Classes="railitem" Command="{Binding ToggleDockModeCommand}"
                                ToolTip.Tip="Dock / undock: switch all modules between the docked host and floating windows">
                            <StackPanel HorizontalAlignment="Center" Spacing="5">
                                <mi:MaterialIcon Classes="rail" Kind="DockWindow"/>
                                <TextBlock Text="{Binding DockToggleLabel}" FontSize="9" LetterSpacing="1"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Classes="railitem" Command="{Binding ToggleCharsCommand}"
                                ToolTip.Tip="Show/hide the character panel">
                            <StackPanel HorizontalAlignment="Center" Spacing="5">
                                <mi:MaterialIcon Classes="rail" Kind="AccountMultipleOutline"/>
                                <TextBlock Text="{Binding CharsToggleLabel}" FontSize="9" LetterSpacing="1"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <!-- Compact Tranquility/status dot for floating mode (the wide bottom bar is hidden there) -->
                        <Border IsVisible="{Binding IsFloating}" HorizontalAlignment="Center" Margin="0,8,0,4"
                                ToolTip.Tip="{Binding RailStatusTooltip}">
                            <Ellipse Width="10" Height="10" Fill="{Binding TranquilityBrush}"/>
                        </Border>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="7"
                                    Margin="0,6,0,9" IsVisible="{Binding ShowRailWindowButtons}">
                            <Button Classes="winbtn" Width="30" Click="OnMinimize" ToolTip.Tip="Minimize">
                                <mi:MaterialIcon Kind="WindowMinimize"/>
                            </Button>
                            <Button Classes="winbtn winclose" Width="30" Click="OnCloseWindow" ToolTip.Tip="Close">
                                <mi:MaterialIcon Kind="Close"/>
                            </Button>
                        </StackPanel>
                    </StackPanel>

                    <!-- Top: module launcher, scrollable so it shrinks instead of drawing over the bottom block when the window is short (ET-84). -->
                    <ScrollViewer x:Name="RailLauncherScroller" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Spacing="2">
                        <!-- HOME (ET-324): the home in front, open tabs left open. Docked only — floating has no host. -->
                        <Button Classes="railitem" Classes.active="{Binding IsHomeShown}" IsVisible="{Binding !IsFloating}"
                                Command="{Binding GoHomeCommand}" ToolTip.Tip="Home (Ctrl+0) — open tabs stay open">
                            <StackPanel HorizontalAlignment="Center" Spacing="5">
                                <mi:MaterialIcon Classes="rail" Kind="HomeOutline"/>
                                <TextBlock Text="HOME" FontSize="9" LetterSpacing="1" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Border Classes="rail-divider" IsVisible="{Binding !IsFloating}"/>
                        <Button Classes="railitem" Classes.active="{Binding IsFitsActive}"
                                Command="{Binding LaunchModuleCommand}" CommandParameter="fits" ToolTip.Tip="Fittings">
                            <StackPanel HorizontalAlignment="Center" Spacing="5">
                                <mi:MaterialIcon Classes="rail" Kind="WrenchOutline"/>
                                <TextBlock Text="FITS" FontSize="9" LetterSpacing="1" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Classes="railitem" Classes.active="{Binding IsFleetActive}"
                                Command="{Binding LaunchModuleCommand}" CommandParameter="fleet" ToolTip.Tip="Fleets">
                            <StackPanel HorizontalAlignment="Center" Spacing="5">
                                <mi:MaterialIcon Classes="rail" Kind="AccountGroupOutline"/>
                                <TextBlock Text="FLEET" FontSize="9" LetterSpacing="1" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Classes="railitem" Classes.active="{Binding IsRunsActive}"
                                Command="{Binding LaunchModuleCommand}" CommandParameter="runs"
                                ToolTip.Tip="Runs — what you flew, and what is running right now">
                            <StackPanel HorizontalAlignment="Center" Spacing="5">
                                <mi:MaterialIcon Classes="rail" Kind="RocketLaunchOutline"/>
                                <TextBlock Text="RUNS" FontSize="9" LetterSpacing="1" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Classes="railitem" Classes.active="{Binding IsCompositionsActive}"
                                Command="{Binding LaunchModuleCommand}" CommandParameter="compositions" ToolTip.Tip="Fleet Compositions">
                            <StackPanel HorizontalAlignment="Center" Spacing="5">
                                <mi:MaterialIcon Classes="rail" Kind="ViewGridOutline"/>
                                <TextBlock Text="COMP" FontSize="9" LetterSpacing="1" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <!-- Tools: a list of stand-alone tools, not one button that happens to have a menu. EVE
                             Settings Sync is the first; more hang under the same flyout as they arrive. -->
                        <Button Classes="railitem" Classes.active="{Binding IsToolsActive}" x:Name="ToolsRailItem"
                                ToolTip.Tip="Tools">
                            <Button.Flyout>
                                <MenuFlyout Placement="Right">
                                    <TextBlock Classes="menuHeader" Text="TOOLS" IsHitTestVisible="False"/>
                                    <MenuItem Header="EVE Settings Sync…" Command="{Binding LaunchModuleCommand}"
                                              CommandParameter="settings-sync"
                                              ToolTip.Tip="Copy EVE's window layout, overview and account settings from one character or account to your others — with a backup taken first.">
                                        <MenuItem.Icon><mi:MaterialIcon Kind="SwapHorizontal"/></MenuItem.Icon>
                                    </MenuItem>
                                    <MenuItem Header="Appraisal…" Command="{Binding LaunchModuleCommand}"
                                              CommandParameter="appraisal"
                                              ToolTip.Tip="Paste what is in a hangar, a wreck or a can and see what it is worth, from the market prices this client already keeps.">
                                        <MenuItem.Icon><mi:MaterialIcon Kind="ScaleBalance"/></MenuItem.Icon>
                                    </MenuItem>
                                    <MenuItem Header="Start Run…" Command="{Binding LaunchModuleCommand}"
                                              CommandParameter="runs-start"
                                              ToolTip.Tip="Start a run by hand: pick a character, an activity and a site, or type one in after the fact.">
                                        <MenuItem.Icon><mi:MaterialIcon Kind="Play"/></MenuItem.Icon>
                                    </MenuItem>
                                </MenuFlyout>
                            </Button.Flyout>
                            <StackPanel HorizontalAlignment="Center" Spacing="5">
                                <mi:MaterialIcon Classes="rail" Kind="ToolboxOutline"/>
                                <TextBlock Text="TOOLS" FontSize="9" LetterSpacing="1" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Border Classes="rail-divider"/>
                        <Button Classes="railitem" Classes.active="{Binding IsEsiActive}"
                                Command="{Binding LaunchModuleCommand}" CommandParameter="esi" ToolTip.Tip="ESI metrics">
                            <StackPanel HorizontalAlignment="Center" Spacing="5">
                                <mi:MaterialIcon Classes="rail" Kind="ChartBar"/>
                                <TextBlock Text="ESI" FontSize="9" LetterSpacing="1" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Classes="railitem" Classes.active="{Binding IsInboxActive}"
                                Command="{Binding LaunchModuleCommand}" CommandParameter="inbox" ToolTip.Tip="Inbox">
                            <StackPanel HorizontalAlignment="Center" Spacing="5">
                                <Panel HorizontalAlignment="Center">
                                    <mi:MaterialIcon Classes="rail" Kind="EmailOutline"/>
                                    <Border IsVisible="{Binding Inbox.HasUnread}" Background="#FFCC4444" CornerRadius="7"
                                            Padding="4,0" MinWidth="14" Height="14" Margin="0,-7,-8,0"
                                            HorizontalAlignment="Right" VerticalAlignment="Top" IsHitTestVisible="False">
                                        <TextBlock Text="{Binding Inbox.UnreadCount}" FontSize="8" FontWeight="Bold"
                                                   Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </Panel>
                                <TextBlock Text="INBOX" FontSize="9" LetterSpacing="1" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Classes="railitem" Classes.active="{Binding IsLogsActive}"
                                Command="{Binding LaunchModuleCommand}" CommandParameter="logs" ToolTip.Tip="Client logs">
                            <StackPanel HorizontalAlignment="Center" Spacing="5">
                                <mi:MaterialIcon Classes="rail" Kind="FileDocumentOutline"/>
                                <TextBlock Text="LOGS" FontSize="9" LetterSpacing="1" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Border Classes="rail-divider"/>
                        <Button Classes="railitem" Command="{Binding LaunchModuleCommand}" CommandParameter="settings"
                                ToolTip.Tip="Settings (Ctrl+,)">
                            <StackPanel HorizontalAlignment="Center" Spacing="5">
                                <mi:MaterialIcon Classes="rail" Kind="TuneVariant"/>
                                <TextBlock Text="SETTINGS" FontSize="9" LetterSpacing="1" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Classes="railitem" Command="{Binding LaunchModuleCommand}" CommandParameter="about"
                                ToolTip.Tip="About EVE Together">
                            <StackPanel HorizontalAlignment="Center" Spacing="5">
                                <mi:MaterialIcon Classes="rail" Kind="InformationOutline"/>
                                <TextBlock Text="ABOUT" FontSize="9" LetterSpacing="1" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    </ScrollViewer>
                </DockPanel>
            </Border>

            <!-- ── Characters ── Docked, a 44 px portrait rail beside every screen (ET-324): a hex per character with a
                 green dot for a client on this PC — absent otherwise, never a grey "offline", since the probe only sees
                 this machine. The cards themselves became the home's pilot rows. Floating has no home, so the narrow
                 shell keeps the full cards. -->
            <Border Grid.Column="1" Classes="rail" IsVisible="{Binding ShowChars}">
                <Panel>
                <DockPanel IsVisible="{Binding !IsFloating}">
                    <Button DockPanel.Dock="Bottom" Classes="railportrait" Margin="0,6,0,10"
                            Command="{Binding AddCharacterCommand}" IsEnabled="{Binding !IsSigningIn}"
                            ToolTip.Tip="Add a character via EVE SSO. Pick which scopes to grant in the dialog.">
                        <Border Width="26" Height="26" BorderThickness="1" BorderBrush="{DynamicResource BorderStrongBrush}">
                            <mi:MaterialIcon Kind="Plus" Width="14" Height="14"/>
                        </Border>
                    </Button>
                    <ScrollViewer VerticalScrollBarVisibility="Hidden">
                        <ItemsControl ItemsSource="{Binding Characters}" Margin="0,8,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="vm:CharacterViewModel">
                                    <Button Classes="railportrait" Margin="0,0,0,6"
                                            Command="{Binding #RootWindow.((vm:MainWindowViewModel)DataContext).OpenCharacterSettingsCommand}"
                                            CommandParameter="{Binding}" ToolTip.Tip="{Binding RailTooltip}">
                                        <Panel Width="30" Height="30">
                                            <ctl:HexPortrait Size="26" Source="{Binding Portrait}" Initial="{Binding Initial}"/>
                                            <Ellipse Width="8" Height="8" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                                     Fill="{StaticResource GreenBrush}" Stroke="{StaticResource BgDeepBrush}"
                                                     StrokeThickness="1.5" IsVisible="{Binding HasActiveClient}"/>
                                        </Panel>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
                <DockPanel IsVisible="{Binding IsFloating}">
                    <Border Classes="panel-header" DockPanel.Dock="Top" Height="40">
                        <Grid ColumnDefinitions="Auto,*,Auto">
                            <TextBlock Grid.Column="0" Classes="diamond" Text="◆" Margin="0,0,6,0"/>
                            <TextBlock Grid.Column="1" Classes="panel-title" Text="CHARACTERS" VerticalAlignment="Center"/>
                            <Button Grid.Column="2" Classes="winbtn" Width="24" Content="«" FontSize="14"
                                    Command="{Binding ToggleCharsCommand}" ToolTip.Tip="Collapse the character panel"/>
                        </Grid>
                    </Border>

                    <StackPanel DockPanel.Dock="Bottom" Margin="8,6" Spacing="6">
                        <Button Content="+ ADD CHARACTER" HorizontalAlignment="Stretch"
                                Command="{Binding AddCharacterCommand}" IsEnabled="{Binding !IsSigningIn}"
                                ToolTip.Tip="Add a character via EVE SSO. Pick which scopes to grant in the dialog."/>
                        <Button Content="CANCEL SIGN-IN" HorizontalAlignment="Stretch"
                                Command="{Binding CancelSignInCommand}" IsVisible="{Binding IsSigningIn}"/>
                    </StackPanel>

                    <ScrollViewer>
                        <!-- DragDrop.AllowDrop makes the list a drop zone: drag/over/drop reach the code-behind handlers
                             that reorder the dragged character to the dropped-on row and persist the new order. -->
                        <ItemsControl x:Name="CharacterList" ItemsSource="{Binding Characters}" Margin="8"
                                      DragDrop.AllowDrop="True">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="vm:CharacterViewModel">
                                    <Border Classes="panel" Margin="0,0,0,8" Padding="9"
                                            Background="{StaticResource BgRowBrush}"
                                            ToolTip.Tip="Drag to reorder. The order is saved and used everywhere (metrics, pickers).">
                                        <StackPanel Spacing="9">
                                            <Grid ColumnDefinitions="Auto,*">
                                                <!-- Hex portrait: faction-coloured ring + ESI render (glyph fallback) -->
                                                <Panel Grid.Column="0" Width="52" Height="58" Margin="0,0,10,0">
                                                    <Path Data="M26,0 L52,14.5 L52,43.5 L26,58 L0,43.5 L0,14.5 Z"
                                                          Fill="{DynamicResource HexBrandBrush}"/>
                                                    <Panel Width="46" Height="52" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                        <Panel.Clip>
                                                            <PathGeometry Figures="M23,0 L46,13 L46,39 L23,52 L0,39 L0,13 Z"/>
                                                        </Panel.Clip>
                                                        <Border Background="{DynamicResource AccentSoftBrush}"
                                                                IsVisible="{Binding !HasPortrait}">
                                                            <TextBlock Text="{Binding Initial}" FontSize="20" FontWeight="Bold"
                                                                       Foreground="{DynamicResource AccentBrightBrush}"
                                                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <Image Source="{Binding Portrait}" Stretch="UniformToFill"
                                                               IsVisible="{Binding HasPortrait}"/>
                                                    </Panel>
                                                </Panel>
                                                <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13"
                                                                   Foreground="{StaticResource TextBrightBrush}"/>
                                                        <!-- Local-client presence: a running EVE client was detected for this
                                                             character on this machine (EveClientPresenceService sweep). -->
                                                        <TextBlock Text="●" FontSize="9" VerticalAlignment="Center"
                                                                   Foreground="#FF6FCF97" IsVisible="{Binding HasActiveClient}"
                                                                   ToolTip.Tip="{Binding ClientStatusTooltip}"/>
                                                    </StackPanel>
                                                    <TextBlock Text="{Binding Affiliation}" FontSize="11" Opacity="0.6"
                                                               TextTrimming="CharacterEllipsis"
                                                               Foreground="{StaticResource TextBrightBrush}"/>
                                                    <!-- Status chips (module-shell mockup "ESI ✓" / "☁ ET"): ESI link state,
                                                         one chip per coupled server, and the implant count. Each chip's
                                                         state symbol used to be a character inside its label — two of
                                                         them emoji, drawn in colour out of a different font (ET-74). -->
                                                    <StackPanel Orientation="Horizontal" Spacing="5" Margin="0,4,0,0">
                                                        <Border Classes="chip" Classes.ok="{Binding EsiOk}" Classes.warn="{Binding EsiWarn}"
                                                                Classes.dim="{Binding EsiDim}" ToolTip.Tip="{Binding EsiStatus}">
                                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                                <TextBlock Text="{Binding EsiChipText}" VerticalAlignment="Center"/>
                                                                <mi:MaterialIcon Kind="{Binding EsiChipIcon}"/>
                                                            </StackPanel>
                                                        </Border>
                                                        <ItemsControl ItemsSource="{Binding ServerLinks}">
                                                            <ItemsControl.ItemsPanel>
                                                                <ItemsPanelTemplate>
                                                                    <StackPanel Orientation="Horizontal" Spacing="5"/>
                                                                </ItemsPanelTemplate>
                                                            </ItemsControl.ItemsPanel>
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate x:DataType="vm:ServerLinkViewModel">
                                                                    <Border Classes="chip" Classes.warn="{Binding HasIssue}"
                                                                            Classes.danger="{Binding HasExpired}"
                                                                            ToolTip.Tip="{Binding LinkTooltip}">
                                                                        <StackPanel Orientation="Horizontal" Spacing="4">
                                                                            <mi:MaterialIcon Kind="{Binding ChipIcon}"/>
                                                                            <TextBlock Text="{Binding DisplayName}" MaxWidth="92"
                                                                                       VerticalAlignment="Center"
                                                                                       TextTrimming="CharacterEllipsis"/>
                                                                        </StackPanel>
                                                                    </Border>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                        <!-- Implant count (D-181): hover lists the character's implants -->
                                                        <Border Classes="chip" IsVisible="{Binding HasImplants}"
                                                                ToolTip.Tip="{Binding ImplantTooltip}">
                                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                                <mi:MaterialIcon Kind="Needle"/>
                                                                <TextBlock Text="{Binding ImplantBadge}" VerticalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Border>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Grid>
                                            <Grid ColumnDefinitions="*,*,*" ColumnSpacing="5">
                                                <Button Grid.Column="0" Padding="0,3"
                                                        HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"
                                                        Command="{Binding #RootWindow.((vm:MainWindowViewModel)DataContext).OpenMetricsCommand}"
                                                        CommandParameter="{Binding}"
                                                        ToolTip.Tip="Open metrics: live DPS, bounty, location, enemies.">
                                                    <mi:MaterialIcon Kind="ChartBar"/>
                                                </Button>
                                                <Button Grid.Column="1" Padding="0,3"
                                                        HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"
                                                        Command="{Binding #RootWindow.((vm:MainWindowViewModel)DataContext).OpenCharacterDpsOverlayCommand}"
                                                        CommandParameter="{Binding}"
                                                        ToolTip.Tip="Pop this character's live DPS into a floating overlay.">
                                                    <mi:MaterialIcon Kind="ArrowExpand"/>
                                                </Button>
                                                <Button Grid.Column="2" Padding="0,3"
                                                        HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"
                                                        Command="{Binding #RootWindow.((vm:MainWindowViewModel)DataContext).OpenCharacterSettingsCommand}"
                                                        CommandParameter="{Binding}"
                                                        ToolTip.Tip="Character settings: ESI scopes, coupled servers, couple/decouple.">
                                                    <mi:MaterialIcon Kind="CogOutline"/>
                                                </Button>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
                </Panel>
            </Border>

            <!-- ── Module host: tabbed header + the docked "fits" home (library + live DPS + shared demo) ── -->
            <DockPanel Grid.Column="2" IsVisible="{Binding ShowHost}">

                <!-- Host header: open-module tab strip (or HOME) + overflow + dock/undock toggle (D-182, ET-157).
                     Fixed height matches the CHARACTERS header so the two column headers line up exactly. -->
                <Border Classes="panel-header" DockPanel.Dock="Top" Height="40" Padding="12,0">
                    <Grid ColumnDefinitions="*,Auto,Auto,Auto">
                        <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center"
                                    IsVisible="{Binding !HasHostTabs}">
                            <TextBlock Classes="diamond" Text="◆"/>
                            <TextBlock Text="HOME" Classes="panel-title"/>
                        </StackPanel>
                        <!-- A horizontal StackPanel has no overflow of its own — tabs that don't fit are clipped by the
                             ListBox's own ScrollViewer and become unreachable (ET-157). The overflow button to its
                             right (below) tracks which tabs fall outside HostTabsList's own width and parks those
                             there instead, rather than adding horizontal scroll. -->
                        <ListBox x:Name="HostTabsList" Grid.Column="0" Classes="hosttabs" IsVisible="{Binding HasHostTabs}"
                                 ItemsSource="{Binding HostTabs}" SelectedItem="{Binding SelectedHostTab}">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemTemplate>
                                <DataTemplate x:DataType="dlg:HostTab">
                                    <StackPanel Orientation="Horizontal" Spacing="7" VerticalAlignment="Center">
                                        <!-- Per screen, not per rail group (ET-171): one fleet can account for three
                                             tabs and all three titles start with the same word. -->
                                        <mi:MaterialIcon Classes="icon-sm" Kind="{Binding Icon}"/>
                                        <TextBlock Text="{Binding Title}" FontSize="11" VerticalAlignment="Center"/>
                                        <Button Classes="tabclose" Command="{Binding CloseCommand}"
                                                ToolTip.Tip="Close this module (Ctrl+W)">
                                            <mi:MaterialIcon Classes="icon-sm" Kind="Close"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <!-- Overflow (ET-157): a fixed width regardless of whether it currently has anything to show,
                             so toggling it never changes HostTabsList's own available width — that would flip tabs in
                             and out of overflow on every layout pass. Dim + no count = nothing hidden right now. -->
                        <Button x:Name="TabOverflowButton" Grid.Column="1" Classes="winbtn" Width="30" Margin="6,0,0,0"
                                IsVisible="{Binding HasHostTabs}" IsEnabled="False" Opacity="0.35"
                                ToolTip.Tip="Tabs that don't fit in the strip">
                            <StackPanel Orientation="Horizontal" Spacing="2">
                                <mi:MaterialIcon Classes="icon-sm" Kind="ChevronDown"/>
                                <TextBlock x:Name="TabOverflowCountText" FontSize="9" VerticalAlignment="Center"/>
                            </StackPanel>
                            <Button.Flyout>
                                <Flyout Placement="BottomEdgeAlignedRight">
                                    <ListBox x:Name="TabOverflowList" Classes="hosttabs-overflow"
                                             SelectionChanged="OnTabOverflowSelectionChanged">
                                        <ListBox.ItemTemplate>
                                            <DataTemplate x:DataType="dlg:HostTab">
                                                <Grid ColumnDefinitions="Auto,*,Auto" MinWidth="180">
                                                    <mi:MaterialIcon Grid.Column="0" Classes="icon-sm" Kind="{Binding Icon}"
                                                                     Margin="0,0,7,0"/>
                                                    <TextBlock Grid.Column="1" Text="{Binding Title}" FontSize="11"
                                                               VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                                    <Button Grid.Column="2" Classes="tabclose" Command="{Binding CloseCommand}"
                                                            ToolTip.Tip="Close this module (Ctrl+W)">
                                                        <mi:MaterialIcon Classes="icon-sm" Kind="Close"/>
                                                    </Button>
                                                </Grid>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                </Flyout>
                            </Button.Flyout>
                        </Button>
                        <TextBlock Grid.Column="2" Text="{Binding DockModeLabel}" Classes="label" FontSize="9"
                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <Button Grid.Column="3" Classes="winbtn" Width="30"
                                Command="{Binding ToggleDockModeCommand}"
                                ToolTip.Tip="Dock / undock: switch all modules between the docked host and floating windows.">
                            <mi:MaterialIcon Kind="DockWindow"/>
                        </Button>
                    </Grid>
                </Border>

                <!-- Host body: the home (default) with a navigated module hosted on top (D-182) -->
                <Grid>
                <Panel IsVisible="{Binding IsHomeShown}">
                    <home:HomeDashboardView DataContext="{Binding Home}"/>
                </Panel>

                <!-- The active module tab's content, hosted over the home (docked tabs, D-182) -->
                <ContentControl Content="{Binding SelectedHostTab.Content}" IsVisible="{Binding !IsHomeShown}"/>
                </Grid>
            </DockPanel>
        </Grid>
    </DockPanel>
</Window>
