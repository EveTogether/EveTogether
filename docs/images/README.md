# README images

These are **placeholders** — replace them with real screenshots, keeping the same filenames so the
README picks them up automatically:

- `home-dashboard.png` — the home dashboard (your characters / fleets / latest fits).
- `fleet.png` — the fleet view with live metrics.

Aim for roughly 1280×800 (16:10) so the side-by-side layout in the README stays even.
