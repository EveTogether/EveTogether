namespace EveUtils.Server.DataExplorer;

/// <summary>The lists on the Data side of the panel; each Dashboard tile and attention item belongs to one.</summary>
public enum DataEntity
{
    Characters = 0,
    Fleets = 1,
    Compositions = 2,
    SharedFits = 3,
    Runs = 4,
    Sessions = 5,
}
