namespace EveUtils.Server.DataExplorer;

/// <summary>What a segment's colour means, so the palette is decided in CSS and not in the query.</summary>
public enum SegmentTone
{
    Good = 0,
    Accent = 1,
    Neutral = 2,
    Warning = 3,
    Bad = 4,
}
